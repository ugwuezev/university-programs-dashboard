import React from 'react'

const KeywordHits = ({ keywords, universities, tweets }) => {

    // console.log(keywords);
    // console.log(universities);
    // console.log(tweets);

  return (
    <div>Keyword Hits</div>
  )
}

export default KeywordHits